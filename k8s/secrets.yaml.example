apiVersion: v1
kind: Secret
metadata:
  name: bobrossbot-secrets
  namespace: default
  labels:
    app: bobrossbot
stringData:
  DISCORD_TOKEN: "<REPLACE_WITH_DISCORD_TOKEN>"
  OPENAI_API_KEY: "<REPLACE_WITH_OPENAI_API_KEY>"

# Do NOT commit real secrets. Use `kubectl create secret` or your secret manager to create this in-cluster.